# Django Environment Variables
# Environment configuration for Django applications

# Django settings
DEBUG=True
SECRET_KEY=your-secret-key-change-this

# Allowed hosts (comma-separated)
# For development, * allows all hosts
# For production, set specific hostnames
DJANGO_ALLOWED_HOSTS=*,localhost,127.0.0.1

# Database configuration (optional)
# DATABASE_URL=postgres://user:password@host:5432/dbname

# CSRF settings (NoHands will set CSRF_TRUSTED_ORIGINS automatically at runtime)
